kaku("Informe um numero natural: ");

yomu(x);

mochi ( x < 0 ) {
    kaku ("Numero invalido");
} soreigai {

    resultado := 1;
    
    tameni i kara 1 made x ho 1 {

        resultado := resultado * i;

    }

    kaku (x, "! = ", resultado);

}